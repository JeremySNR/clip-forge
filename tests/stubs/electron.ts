/**
 * Electron stub for unit tests. Production modules only touch `app` behind
 * optional chaining (they already run outside Electron in the test scripts),
 * so exporting undefined mirrors that environment.
 */
export const app = undefined
